# simplified process for running:
---> bas bat script ko execute kro
#NOTE: 
1)before executing make sure you have created a virtual environment and installed requirements in the venv
2)put the .env file (sent in discord) inside the backend folder

ab tum local host ke links ke through backend frontend dono dekh skte ho
